[project]
name = "sdpcodec"
version = "0.1.0"
description = "Clean SDPCodec training entrypoint for the HuBERT RVQ6561 baseline."
requires-python = ">=3.10"

[tool.setuptools]
packages = ["sdpcodec"]
